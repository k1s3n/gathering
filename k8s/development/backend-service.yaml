# backend-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: development
spec:
  type: LoadBalancer # or ClusterIP
  selector:
    app: backend 
  ports:
    - protocol: TCP
      port: 4000
      targetPort: 4000
