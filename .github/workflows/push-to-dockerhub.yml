name: CI/CD Pipeline

# Trigger the workflow when pushing to the christoffer branch
on:
  push:
    branches:
      - Christoffer
      #ddd

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # Check out the repository
      - name: Check out repository
        uses: actions/checkout@v3

      # Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # Build the Docker image
      - name: Build Docker image
        run: docker build -t muschan/frontend:latest ./frontend

      # Push the Docker image to Docker Hub
      - name: Push Docker image
        run: docker push -t muschan/frontend:latest

      # Update Kubernetes deployment
      #- name: Update Kubernetes deployment
      #  env:
      #    KUBECONFIG: ${{ secrets.KUBECONFIG }}
      #  run: |
      #    kubectl set image deployment/frontend frontend=your-dockerhub-username/frontend:latest -n your-namespace
